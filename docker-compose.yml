version: '3.8'
services:
  frontend:
    build: ./frontend
    container_name: karibu_frontend
    ports:
      - "4173:4173"
    environment:
      - NODE_ENV=production
    volumes:
      - ./frontend:/app
      - /app/node_modules
    restart: unless-stopped
